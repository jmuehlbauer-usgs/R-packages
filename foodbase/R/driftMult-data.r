#' Size bin and mesh size multiplers for comparing drift samples with differing mesh size.
#'
#' Data on the proportion of total sample counts represented by each size bin for drift samples.
#' Also the multiplers used that vary from 250 to 500 um mesh nets.
#' Used internally for converting to/from 250 and 500 um mesh nets.
#'
#' @docType data
#'
#' @usage data(driftMultipliers)
#'
#' @format An object of class \code{"list"}.
#'
#' @keywords datasets
#'
#' @examples
#' data(driftMultipliers)
#' foo <- driftMult
"driftMult"
